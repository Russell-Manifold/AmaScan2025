<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:AmaScan.Controls"
             x:Class="AmaScan.StockCountSearchPage"
             Title="Search Stock Count Item">

    <Grid RowDefinitions="Auto,*" Padding="20">
        <!-- Header -->
        <Frame Grid.Row="0" Padding="10" BackgroundColor="LightGrey" Margin="0,0,0,10">
            <VerticalStackLayout>
                <Label Text="Items to count" FontSize="18" FontAttributes="Bold"/>
                <Label Text="Tap an item to start counting." FontSize="12"/>
            </VerticalStackLayout>
        </Frame>

        <!-- Items List -->
        <CollectionView Grid.Row="1"
                        x:Name="ItemsListView"
                        ItemsSource="{Binding Items}"
                        SelectionMode="Single"
                        SelectionChanged="OnItemSelected">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Padding="10" Margin="10,5"
                           BackgroundColor="White"
                           BorderColor="LightGray"
                           CornerRadius="5">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnItemTapped"
                                                  CommandParameter="{Binding}" />
                        </Frame.GestureRecognizers>
                        <Grid ColumnDefinitions="*,Auto">
                            <VerticalStackLayout Grid.Column="0">
                                <Label Text="{Binding StockDescription}"
                                       FontAttributes="Bold"/>
                                <Label Text="{Binding StockCode}"
                                       FontSize="12"/>
                                <Label Text="{Binding BarCode}"
                                       FontSize="11"
                                       TextColor="SteelBlue"/>
                                <Label Text="{Binding BarcodeLmmp}"
                                       FontSize="11"
                                       TextColor="SteelBlue"
                                       IsVisible="{Binding BarcodeLmmp, Converter={StaticResource StringNotEmpty}}"/>
                            </VerticalStackLayout>
                            <Label Grid.Column="1"
                                   Text="›"
                                   FontSize="24"
                                   TextColor="Gray"
                                   VerticalOptions="Center"/>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Loading Indicator -->
        <ActivityIndicator Grid.Row="1"
                          x:Name="LoadingIndicator"
                          IsRunning="{Binding IsLoading}"
                          IsVisible="{Binding IsLoading}"
                          HorizontalOptions="Center"
                          VerticalOptions="Center"/>
    </Grid>
</ContentPage>